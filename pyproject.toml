[project]
name = "netplanner-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.135.1",
    "folium>=0.20.0",
    "mlflow>=3.10.1",
    "pytest>=9.0.2",
    "uvicorn>=0.42.0",
]
